---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: csi-test-ceph-sts
  namespace: test
spec:
  serviceName: csi-test-ceph-sts
  replicas: 1
  selector:
    matchLabels:
      app: csi-test-ceph-sts
  template:
    metadata:
      labels:
        app: csi-test-ceph-sts
    spec:
      containers:
        - name: writer
          image: busybox:1.37
          command:
            - sh
            - -c
            - 'date >> /data/log; tail -f /data/log'
          volumeMounts:
            - name: data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        storageClassName: rook-ceph-block
        resources:
          requests:
            storage: 1Gi