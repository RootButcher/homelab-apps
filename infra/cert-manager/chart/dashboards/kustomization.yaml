# Grafana dashboard for cert-manager.
# Source: https://grafana.com/grafana/dashboards/20842-cert-manager-kubernetes/ (revision 3)
# (The previous imusmanmalik/cert-manager-mixin JSON was malformed upstream.)
# Refresh: re-download a newer revision and bump the revision number above.
# TODO updater
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: cert-manager-dashboards
    files:
      - cert-manager-kubernetes.json
    options:
      disableNameSuffixHash: true
      labels:
        grafana_dashboard: "1"
