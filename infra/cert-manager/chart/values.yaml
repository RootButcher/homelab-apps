crds:
  enabled: true

replicaCount: 1

prometheus:
  enabled: false

# Split DNS workaround.
extraArgs:
  - --dns01-recursive-nameservers-only
  - --dns01-recursive-nameservers=1.0.0.1:53,8.8.8.8:53,1.1.1.1:53
