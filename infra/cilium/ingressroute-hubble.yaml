apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hubble-ui
  namespace: kube-system
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`hubble.local.rootbutcher.dev`)
      kind: Rule
      services:
        - name: hubble-ui
          port: 80
  tls: {}
